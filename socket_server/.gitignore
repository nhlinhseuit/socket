# 👉 Thư mục chứa dependency
node_modules/

# 👉 File môi trường & bí mật
.env
.env.local
.env.*.local

# 👉 Log & debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# 👉 Build / output (nếu có dùng TypeScript)
dist/
build/
*.tsbuildinfo

# 👉 VSCode settings
.vscode/

# 👉 OS file
.DS_Store
Thumbs.db

# 👉 Lockfile (tuỳ team dùng yarn, npm hay pnpm)
# Nếu bạn dùng npm: giữ lại `package-lock.json`, ignore cái khác
yarn.lock
pnpm-lock.yaml
